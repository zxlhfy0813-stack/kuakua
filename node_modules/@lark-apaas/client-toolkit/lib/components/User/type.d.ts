import { IUserProfile } from '../../apis/udt-types';
export interface UserWithAvatarProps {
    data: IUserProfile;
    /**
     * @default medium
     */
    size?: 'small' | 'medium' | 'large';
    /**
     * @default tag
     */
    mode?: 'tag' | 'plain';
    className?: string;
    /**
     * @default true
     */
    showLabel?: boolean;
}
